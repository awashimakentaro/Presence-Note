# .env.template
#
# 【責務】
# Presence Note の Python プロセスが参照する環境変数セットの雛形を提供する。
#
# 【使用箇所】
# - プロジェクトルートで `.env` を作成するときのコピー元。
# - `app/environment.py` が `python-dotenv` 経由で読み込む想定。
#
# 【やらないこと】
# - 実際のシークレット値の管理
# - 自動生成や暗号化
#
# 【他ファイルとの関係】
# - `app/config.py` が読み取るキーと 1:1 に対応する。
# - systemd 用テンプレート (systemd/ntfy-print.env.template) と値を共有する。
#
# このファイルをコピーして `.env` を作り、値を埋めてください。

NTFY_TOPIC_URL=https://ntfy.sh/YOUR_TOPIC
PRINTER_NAME=YOUR_PRINTER_NAME
NTFY_TOKEN=
